choco install chocolateygui
choco install tim
choco install everything
choco install potplayer
choco install MobaXterm

# 作用：
# 开发用途，例如golang，nodejs，vscode
# 便于批量部署；CLI接口，可以用于部署win服务器；
# 如果介意原本环境，注册表洁癖，适合使用scoop